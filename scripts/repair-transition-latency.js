const fs=require("node:fs");const path=require("node:path");function replaceOnce(source,oldValue,newValue,label){const count=source.split(oldValue).length-1;if(count!==1)throw new Error(`${label}: expected exactly 1 match, found ${count}`);return source.replace(oldValue,newValue);}

const teacherTarget=path.join(process.cwd(),"app","teacher","assessment","AssessmentClient.jsx");const teacherMarker="CRL_TRANSITION_LATENCY_REPAIR";let teacherSource=fs.readFileSync(teacherTarget,"utf8");if(!teacherSource.includes(teacherMarker)){let patched=teacherSource;patched=replaceOnce(patched,"      try {\n        const data = await persistAnswerWithRetry(\n          \"record_word\",","      const optimisticStorySession = {\n        ...(latestSessionRef.current || {}), code, stage: \"story_choice\", current_content: \"\", currentContent: \"\", connected: true,\n      };\n      latestSessionRef.current = optimisticStorySession; latestActiveStageRef.current = \"story_choice\"; latestSessionVersionRef.current = Date.now(); setSession(optimisticStorySession); setActiveStage(\"story_choice\"); publishAssessmentState(assessmentChannelRef.current,{source:\"teacher\",session:optimisticStorySession}); void publishAssessmentRealtimeState(code,optimisticStorySession);\n\n      try {\n        const data = await persistAnswerWithRetry(\n          \"record_word\",","Final Word -> Story Choice optimistic transition");patched=replaceOnce(patched,"      latestSessionRef.current = optimistic;\n      latestActiveStageRef.current = \"passage\";\n      latestSessionVersionRef.current = Date.now();\n      setSession(optimistic);\n      setActiveStage(\"passage\");\n\n      try {","      latestSessionRef.current = optimistic;\n      latestActiveStageRef.current = \"passage\";\n      latestSessionVersionRef.current = Date.now();\n      setSession(optimistic);\n      setActiveStage(\"passage\");\n      publishAssessmentState(assessmentChannelRef.current,{source:\"teacher\",session:optimistic});\n      void publishAssessmentRealtimeState(code,optimistic);\n\n      try {","Immediate story passage realtime");patched=replaceOnce(patched,"          setQuestionIndex(0);\n          setSession(nextSession);\n          setActiveStage(\"comprehension\");\n          void publishAssessmentRealtimeState(code, nextSession);","          setQuestionIndex(0);\n          setSession(nextSession);\n          setActiveStage(\"comprehension\");\n          publishAssessmentState(assessmentChannelRef.current,{source:\"teacher\",session:nextSession});\n          void publishAssessmentRealtimeState(code,nextSession);","Immediate first comprehension realtime");patched=patched.replace("{/* CRL_MISCUE_MARKING_REPAIR */}","{/* CRL_MISCUE_MARKING_REPAIR CRL_TRANSITION_LATENCY_REPAIR */}");fs.writeFileSync(teacherTarget,patched,"utf8");}

const learnerTarget=path.join(process.cwd(),"app","learner","LearnerAssessmentPage.jsx");const learnerMarker="CRL_TRANSITION_LATENCY_LEARNER_FALLBACK";let learnerSource=fs.readFileSync(learnerTarget,"utf8");if(!learnerSource.includes(learnerMarker)){let patched=learnerSource;patched=replaceOnce(patched,"      const fastLiveStage =\n        liveStage === \"letter\" ||\n        liveStage === \"word\";","      const fastLiveStage =\n        liveStage === \"letter\" ||\n        liveStage === \"word\" ||\n        liveStage === \"story_choice\" ||\n        liveStage === \"passage\" ||\n        liveStage === \"comprehension\";","Learner active-stage fallback polling");patched=patched.replace("  // Cross-device realtime subscription is primary; polling is a fallback.",`  /* ${learnerMarker} */\n  // Cross-device realtime subscription is primary; polling is a fallback.`);fs.writeFileSync(learnerTarget,patched,"utf8");}

const routeTarget=path.join(process.cwd(),"app","api","assessment","route.js");const routeMarker="CRL_STORY_SELECTION_TIMER_OVERLAY_REPAIR_ROUTE";let routeSource=fs.readFileSync(routeTarget,"utf8");if(!routeSource.includes(routeMarker)){let patched=routeSource;patched=replaceOnce(patched,`          stage: "passage",\n          currentContent: selected.text,\n          storyTitle: selected.title,\n          passageStartedAt: null,\n          passagePausedAt: null,\n          passagePausedSeconds: 0,`,`          stage: "passage",\n          currentContent: selected.text,\n          storyTitle: selected.title,\n          passageStartedAt: new Date(),\n          passagePausedAt: null,\n          passagePausedSeconds: 0,`,`Select story timer start`);patched=patched.replace('    if (action === "select_story") {',`    /* ${routeMarker} */\n    if (action === "select_story") {`);fs.writeFileSync(routeTarget,patched,"utf8");}

const storyRepairTeacherMarker="CRL_STORY_SELECTION_TIMER_OVERLAY_REPAIR_TEACHER";let storyRepairTeacherSource=fs.readFileSync(teacherTarget,"utf8");if(!storyRepairTeacherSource.includes(storyRepairTeacherMarker)){let patched=storyRepairTeacherSource;patched=replaceOnce(patched,`  const [storySelecting, setStorySelecting] = useState(false);\n  const [passagePaused, setPassagePaused] = useState(false);`,`  const [storySelecting, setStorySelecting] = useState(false);\n  const [showWordSavingOverlay, setShowWordSavingOverlay] = useState(false);\n  const [passagePaused, setPassagePaused] = useState(false);`,`Saving overlay state`);patched=replaceOnce(patched,`      const optimistic = {\n        ...(latestSessionRef.current || {}),\n        ...next,\n        connected: true,\n      };`,`      const passageStartedAt = new Date().toISOString();\n      const optimistic = {\n        ...(latestSessionRef.current || {}),\n        ...next,\n        connected: true,\n        passage_started_at: passageStartedAt,\n        passageStartedAt,\n        passage_paused_at: null,\n        passagePausedAt: null,\n        passage_paused_seconds: 0,\n        passagePausedSeconds: 0,\n      };`,`Local passage timer state`);patched=replaceOnce(patched,`      setSession(optimistic);\n      setActiveStage("passage");\n\n      try {`,`      setSession(optimistic);\n      setActiveStage("passage");\n      publishAssessmentState(assessmentChannelRef.current,{source:"teacher",session:optimistic});\n      void publishAssessmentRealtimeState(code,optimistic);\n\n      try {`,`Immediate selected-story broadcast`);patched=replaceOnce(patched,`      try {\n        const data = await persistAnswerWithRetry(\n          "record_word",`,`      setShowWordSavingOverlay(true);\n      try {\n        const data = await persistAnswerWithRetry(\n          "record_word",`,`Final-word save overlay on`);patched=replaceOnce(patched,`        setBusy(false);\n        setTransitionPending(false);\n      }\n    };\n\n  const recordComprehension =`,`        setBusy(false);\n        setTransitionPending(false);\n        setShowWordSavingOverlay(false);\n      }\n    };\n\n  const recordComprehension =`,`Final-word save overlay off`);patched=replaceOnce(patched,`        {reversionSelecting && reversionSourceWord && (`,`        {showWordSavingOverlay && (\n          <div style={{position:"fixed",inset:0,zIndex:7000,display:"flex",alignItems:"center",justifyContent:"center",padding:"24px",background:"rgba(12,32,52,.62)",backdropFilter:"blur(10px)",WebkitBackdropFilter:"blur(10px)"}} role="status" aria-live="assertive" aria-label="Saving assessment">\n            <div style={{width:"min(460px,92vw)",padding:"36px 30px",border:"1px solid #d4e2ed",borderRadius:"24px",background:"linear-gradient(145deg,#f8fbff,#eaf3f9)",boxShadow:"16px 18px 40px rgba(7,25,42,.28),-8px -8px 18px rgba(255,255,255,.78)",textAlign:"center"}}>\n              <div aria-hidden="true" style={{width:"42px",height:"42px",margin:"0 auto 18px",border:"4px solid rgba(21,89,166,.18)",borderTopColor:"#1559a6",borderRadius:"50%",animation:"crlAssessmentSpin .72s linear infinite"}}/>\n              <h2 style={{margin:0,color:"#193c5b",fontSize:"28px",fontWeight:"950"}}>Saving, please wait...</h2>\n              <p style={{margin:"10px 0 0",color:"#6d8498",fontSize:"14px",lineHeight:1.6}}>Saving the final Word Recognition result before opening the story passage choices.</p>\n            </div>\n          </div>\n        )}\n\n        {reversionSelecting && reversionSourceWord && (`,`Final-word full-screen saving overlay`);patched=patched.replace("      <main className=\"teacherAssessmentPage\"",`      {/* ${storyRepairTeacherMarker} */}\n      <main className="teacherAssessmentPage"`);fs.writeFileSync(teacherTarget,patched,"utf8");}
